const ActiveMatrix = () => {
	return (
		<p className="absolute bottom-4 select-none font-medium text-sm text-center w-screen xl:w-[40dvw]">
			From Active Matrix
		</p>
	);
};

export default ActiveMatrix;
