const ActiveMatrix = () => {
	return (
		<p className="absolute bottom-4 select-none font-medium text-sm text-center w-full xl:w-[40dvw] transform-gpu">
			From Active Matrix
		</p>
	);
};

export default ActiveMatrix;
